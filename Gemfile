# Solo para previsualizar el sitio en local:
#   bundle install && bundle exec jekyll serve
# GitHub Pages no usa este archivo: construye el sitio con su propia version.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
